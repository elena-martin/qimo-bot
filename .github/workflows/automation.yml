on: push
jobs:
  first-job:
    runs-on: ubuntu-latest
    container: node:16.14.0
    steps:
     - run: node --version
     - run: npm --version
     - uses: actions/checkout@v3
     - run: npm install
     